import styled from 'styled-components';

const WrapperStyles = styled.main`
  width: 100%;
  min-height: 100vh;
  background: ${({ theme }) => theme.VeryDarkBlue};
  padding: 3em 0 3em 0;
  font-family: Rubik;
`;

export default WrapperStyles;
